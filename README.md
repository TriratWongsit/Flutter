# Flutter# Flutter Navigation & Material Widgets Showcase

**รายวิชา:** Mobile Application and Development
**จัดทำโดย:** ตรัยรัตน์ วงษ์สิทธิ์ 67543210028-6

📌 รายละเอียดโปรเจกต์ (Project Description)
แอปพลิเคชันนี้พัฒนาขึ้นเพื่อศึกษาและปฏิบัติการเรื่อง Navigation and Routing ใน Flutter Framework โดยนำเนื้อหาเรื่อง Material Component Widgets มาจัดหมวดหมู่และแสดงผลในรูปแบบแอปพลิเคชันที่มีการเชื่อมโยงหน้าจอหลายหน้า (Multi-screen Application)

แอปพลิเคชันนี้มีความสามารถหลักคือ:

แสดงรายการ Widget พื้นฐานแยกตามหมวดหมู่ (Actions, Communication, Containment, etc.)

สามารถกดเลือกเมนูเพื่อเปลี่ยนหน้าจอไปยังหน้าย่อยได้ (Navigation)

ในแต่ละหน้าย่อย สามารถดูผลลัพธ์หน้าจอ (UI Preview) และดูโค้ดตัวอย่าง (Source Code) ได้ทันที

🛠 การออกแบบและเทคนิคที่ใช้ (Architecture & Implementation)
1. การจัดการ Navigation (Routing)
โปรเจกต์นี้ใช้เทคนิค Named Routes ในการจัดการเส้นทาง เพื่อให้โค้ดอ่านง่ายและเป็นระเบียบ โดยมีการตั้งค่าใน MaterialApp ที่ไฟล์ main.dart ดังนี้:

Routes Map:

Dart
routes: {
  '/': (context) => const HomePage(),
  '/actions': (context) => const ActionsPage(),
  '/communication': (context) => const CommunicationPage(),
  // ... และเส้นทางอื่นๆ
},
Navigation: ใช้คำสั่ง Navigator.pushNamed(context, '/ชื่อเส้นทาง') เมื่อผู้ใช้กดเลือกเมนูในหน้าแรก เพื่อเดินทางไปยังหน้าต่างๆ

## พรุ่งนี้ ผมมาใส่รูป เพิ่ม ตอนนี้ขอไปเล่น Dota ก่อน เย้ยยยยย
